# AISLENS SLURM Jobs

This directory contains the historical SLURM workflow documentation and archived job scripts for AISLENS.

The currently active workflow lives in [`refactored/`](refactored/). Start there for the scripts that are meant to be used now.

## Contents now:

- [`refactored/`](refactored/) - current consolidated SLURM scripts and config
- [`legacy-slurm-workflow.md`](legacy-slurm-workflow.md) - full reference for the older workflow and script history
- [`archive/`](archive/) - archived scripts and legacy variants

## For running jobs:

Use the scripts in `refactored/`.

## To understand the legacy workflow:

Read [`legacy-slurm-workflow.md`](legacy-slurm-workflow.md).

